/** @type {import('next').NextConfig} */

// Minimal configuration for the Mini-Notion clone
// Additional settings (like image domains) can be added later if needed
const nextConfig = {}

module.exports = nextConfig
